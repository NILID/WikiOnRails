.row
  .col
    %h1= t('pages.listing')
  .col.text-right
    = link_to 'New Page', new_page_path, class: 'btn btn-sm btn-secondary'

.row
  .col
    %ul
      - @pages.each do |page|
        %li
          = link_to page.name, page